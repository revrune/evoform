# Staged thin public door (local / CI artifact)
_public/

# Vercel local link (optional; IDs also in docs)
.vercel/

# macOS
.DS_Store

# Local iteration backups (not the public door)
.local/

# Local print / foil shop (not the public door)
prototypes/
